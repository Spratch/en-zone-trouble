---
import { PortableText as PortableTextInternal } from "astro-portabletext";
const { portableText, class: className } = Astro.props;
---

<div class={className}><PortableTextInternal value={portableText} /></div>
